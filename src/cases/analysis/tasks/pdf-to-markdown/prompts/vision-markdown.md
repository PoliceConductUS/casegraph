# Vision Markdown Extraction

Read rendered PDF page images and return clean Markdown.

Preserve reading order, tables, captions, and page boundaries. Do not summarize,
omit, or add facts. If text is illegible, mark it as `[illegible]` instead of
guessing.
